# 🎯 Darts

Простое приложение на C# Windows Forms для подсчёта счёта в игре дартс по правилам **501** для двух игроков.

---

## 📋 Описание

Приложение позволяет двум игрокам (**Красный** и **Зелёный**) вести счёт в классической игре в дартс "501". Каждый игрок начинает с 501 очка и по очереди вводит результаты трёх бросков. Побеждает тот, кто первым точно обнулит свой счёт.

### 🎮 Правила игры (501)

- Каждый игрок начинает с **501 очка**
- За ход игрок делает **3 броска**, сумма которых вычитается из текущего счёта
- Если после вычитания счёт становится **ровно 0** — игрок побеждает 🏆
- Если сумма бросков **превышает остаток** — происходит "перебор" (bust), ход аннулируется
- Игра автоматически сбрасывается после победы или перебора

---

## ⚙️ Функционал

### Для каждого игрока:
- 🔢 Три текстовых поля для ввода результатов бросков
- 🔘 Кнопка подтверждения хода
- 📊 Отображение текущего счёта
- 🔄 Счётчик сделанных ходов

### Автоматически:
- ✅ Валидация введённых данных (только целые числа)
- ✅ Проверка на "перебор" (bust)
- ✅ Определение победителя
- ✅ Сброс игры после завершения раунда
- ✅ Очистка полей ввода после каждого хода

---

## 🚀 Как запустить

### Требования
- .NET Framework (версия, совместимая с вашим проектом)
- Visual Studio 2019 или новее (рекомендуется)

### Инструкция
1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш-юзернейм/darts.git
   ```
2. Откройте файл `Darts.sln` в Visual Studio
3. Соберите проект: `Ctrl + Shift + B` или через меню **Build → Build Solution**
4. Запустите: `F5` или **Debug → Start Debugging**

---

## 🗂️ Структура проекта

```
Darts/
├── Form1.cs          // Основной код логики игры
├── Form1.Designer.cs // Автоматически сгенерированный код формы
├── Program.cs        // Точка входа в приложение

---

## 💻 Использование

1. Запустите приложение
2. **Красный игрок** вводит результаты трёх бросков в поля `textBox1Red`, `textBox2Red`, `textBox3Red`
3. Нажимает кнопку `buttonRed` для подтверждения хода
4. Приложение обновляет счёт и количество ходов
5. Повторите шаги 2–4 для **Зелёного игрока**
6. Игра продолжается, пока один из игроков не обнулит счёт точно
7. После победы или "перебора" игра автоматически сбрасывается

> ⚠️ **Важно:** Если сумма бросков превышает оставшиеся очки, ход не засчитывается, и счёт остаётся неизменным.

---

## 🛠️ Технологии

- **Язык:** C#
- **Платформа:** Windows Forms (.NET Framework)
- **IDE:** Visual Studio
- **UI:** Русскоязычный интерфейс

---

## ❓ FAQ

**Q: Что делать, если игрок ввёл не число?**  
A: Приложение проигнорирует некорректный ввод — убедитесь, что все три поля заполнены целыми числами.

**Q: Можно ли изменить стартовый счёт?**  
A: В текущей версии стартовый счёт фиксирован — 501. Для изменения потребуется правка кода в полях `scoreRed` и `scoreGreen`.

**Q: Почему игра сбрасывается после перебора?**  
A: Это упрощённая логика. В классических правилах при переборе ход просто аннулируется, но счёт не сбрасывается. Вы можете доработать метод `ResetGame()` под свои нужды.

---

*Сделано с ❤️ для любителей дартса* 🎯
